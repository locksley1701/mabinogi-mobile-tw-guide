# 法那提歐的愛爾琳手札｜資料狀態與 UI 呈現契約

版本：1.0  
適用範圍：公開網站 JSON、搜尋索引、列表卡、詳情頁、更新紀錄與未來玩家投稿資料。

## 1. 核心原則

1. 台版正式名稱與台版遊戲內容優先。
2. 「是否存在／是否實裝」「玩家是否已解鎖」「資料是否已完整確認」是三件不同的事，必須使用不同欄位表示。
3. 已確認存在的內容一律保持正常可閱讀，不得因玩家尚未達到等級而整卡灰暗。
4. 公開網站只呈現整理後資料；原始截圖檔名、Google Drive 連結、私人帳號、電子郵件與審核備註留在私人工作區。
5. 韓國版資料只能標示為參考，不得直接當成台版正式內容。
6. 狀態不能只靠顏色辨認，必須同時提供文字，必要時搭配圖示、邊框或形狀。

## 2. 欄位契約

### `status`

表示「這筆公開資料目前的確認層級」，不得表示玩家是否已學會技能。

允許值：

| 值 | 語意 | 建議公開文字 | UI 原則 |
|---|---|---|---|
| `tw-confirmed` | 已由台版遊戲內容確認 | 台版遊戲資料／台版已確認 | 正常樣式 |
| `user-tested` | 已由玩家實測確認 | 法那提歐實測／玩家實測 | 正常樣式，可顯示實測者 |
| `tw-testing` | 台版項目存在，但部分效果、效率或數值仍待實測 | 台版待實測／資料待補 | 正常可閱讀，不得做成失效 |
| `kr-reference` | 韓國版候選參考，尚未確認台版一致 | 韓國版參考 | 與台版正式內容明確區隔 |
| `unconfirmed` | 目前無法確認 | 待確認 | 明確標示，不得包裝成正式資料 |

禁止使用 `失效`、`未實裝`、`無效` 等文字描述一個已確認存在、只是尚未解鎖或資料尚待補充的項目。

### `statusLabel`

可覆寫公開狀態文字，但必須與 `status` 語意一致。

- `tw-confirmed` 不得搭配「未確認」「失效」「未實裝」。
- `tw-testing` 可使用「資料待補」「效果待實測」，但不得讓整張內容無法閱讀。
- 沒有特殊文案需求時可省略，由網站使用預設文字。

### `unlock`

表示遊戲內解鎖條件，例如：

```json
{"unlock":"劍術士 Lv.45 以上"}
```

規則：

- 解鎖條件是資訊欄位，不是資料可信度或實裝狀態。
- 沒有特殊條件時省略，不必顯示「初始可用」。
- UI 固定使用「解鎖條件：……」呈現。
- 不得因存在 `unlock` 而加入 `disabled`、`locked`、`inactive` 等假狀態。

### `detailStatus`

表示資料仍缺少哪些細節，例如：

```json
{"detailStatus":"技能名稱與介紹已確認，詳細數值待補。"}
```

規則：

- 只描述缺少的資料，不表示內容失效。
- 有正式名稱與介紹時仍使用正常卡片樣式。
- 不得使用「失效」「不存在」等與事實不符的措辭。

### `contributor`

公開署名名稱，例如：

```json
{"contributor":"法那提歐"}
```

規則：

- 由法那提歐整理或實測時，公開顯示「資料整理：法那提歐」或「法那提歐實測」。
- 玩家投稿通過審核後，可填遊戲 ID、自訂暱稱或 `匿名`。
- 不得填入真實姓名、電子郵件、Google 帳號、電話或其他私人聯絡資訊。
- 新資料應明確填寫 `contributor`；既有資料暫時允許由舊 `source` 文字推導署名，但驗證器會提出警告，後續逐步遷移。

### `source`

可保留簡短、公開安全的資料依據，例如：

```json
{"source":"台版遊戲內容"}
```

公開 repository 中禁止包含：

- 原始截圖檔名，例如 `IMG_1551.PNG`
- Google Drive／Google Docs 私人連結
- 電子郵件、電話與私人帳號
- 內部審核表、附件 ID 或工作階段識別碼

網站 UI 不直接顯示原始 `source`；一般使用者只看到整理後的狀態與署名。

### 名稱與別名

- 公開標題始終使用台版正式名稱。
- 舊稱、其他版本名稱與常見俗稱只作搜尋別名。
- 兩個名稱相近但實際不同的項目不得合併。
- 名稱對照不重新成為側邊欄主要頁面。

## 3. 禁止的資料欄位

下列欄位不得用來表示「玩家尚未解鎖」：

- `disabled`
- `inactive`
- `locked`
- `isLocked`
- `unavailable`
- `invalid`

若未來真的需要表示活動已結束、內容暫停供應等狀態，必須另開契約版本並定義清楚語意，不得沿用技能解鎖概念。

## 4. UI 呈現規則

### 列表與卡片

- 已確認存在：正常文字、正常對比。
- 有解鎖條件：額外顯示解鎖資訊，不降低整卡透明度。
- 詳細數值待補：保留名稱與介紹，另外顯示 `detailStatus`。
- 韓國版參考或未確認：必須有清楚文字標籤，不能只靠顏色。

### 搜尋結果

- 顯示台版正式名稱。
- 可透過別名命中，但不把別名取代正式標題。
- 解鎖條件可出現在摘要中。
- 搜尋索引不得把私人來源資訊加入關鍵字。

### 詳情頁

建議順序：

1. 正式名稱
2. 類型／分類
3. 介紹與效果
4. 解鎖條件
5. 待補細節
6. 資料狀態
7. 資料整理者／投稿者

### 更新紀錄

- 記錄公開資料變更，不寫入原始截圖檔名、私人 Drive 路徑或投稿者聯絡資訊。
- 名稱、效果、解鎖條件或狀態更正都需寫入 changelog。

## 5. 代表性範例

### 已確認且有解鎖條件的技能

```json
{
  "name": "尖銳之眼",
  "description": "捕捉對手破綻的卓越眼力。",
  "unlock": "劍術士 Lv.45 以上",
  "status": "tw-confirmed",
  "contributor": "法那提歐"
}
```

### 已確認名稱與介紹，但數值待補

```json
{
  "name": "鞘擊",
  "description": "以劍與劍鞘擊倒敵人的制壓攻擊。",
  "detailStatus": "詳細傷害與冷卻數值待補。",
  "status": "tw-testing",
  "contributor": "法那提歐"
}
```

### 玩家實測內容

```json
{
  "title": "採集掛機路線",
  "status": "user-tested",
  "statusLabel": "法那提歐實測",
  "contributor": "法那提歐"
}
```

### 通過審核的匿名投稿

```json
{
  "title": "任務獎勵補充",
  "status": "tw-confirmed",
  "contributor": "匿名"
}
```

### 韓國版候選參考

```json
{
  "title": "候選料理效果",
  "status": "kr-reference",
  "statusLabel": "韓國版參考",
  "detailStatus": "台版效果尚待確認。"
}
```

## 6. 驗證與遷移

- `scripts/validate-data-contract.mjs` 會檢查允許狀態、矛盾文案、禁止欄位與私人資訊洩漏。
- GitHub Actions 必須在每次 push 與 PR 執行契約驗證。
- 現有資料若缺少明確 `contributor`，先輸出警告；新資料應直接使用正式欄位。
- 契約變更必須更新版本、範例與 changelog。

## 7. 本契約不處理的事項

- 官方圖標裁切與資產輸出規格。
- 投稿表單權限與私人審核後台。
- 裝備、地圖、任務與活動的專用欄位。
- 多配色主題的具體色碼。

上述項目由對應 Issue 另行定義，但不得違反本契約的狀態、署名與資料安全原則。
