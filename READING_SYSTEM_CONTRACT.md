# 全站舒適閱讀字級契約

本文件定義《法那提歐的愛爾琳手札》的文字階層、閱讀尺寸與跨裝置驗收規則。

## 1. 目的

網站是長時間使用的攻略工具，不以展示型小字換取表面精緻。任何內容在手機、平板與桌面上都應保持清楚、可掃讀且不造成持續閱讀疲勞。

## 2. 閱讀尺寸

網站提供三種閱讀尺寸：

- `standard`：標準，保留較高資訊密度。
- `comfortable`：舒適，作為預設值，適合長時間閱讀。
- `large`：放大，提高文字與間距，適合手機或視力需求。

偏好以 `fanatio-reading-size` 保存，並在 `<html data-reading-size>` 套用。

## 3. 文字階層

舒適模式基準：

- 主要正文與技能說明：17px，行距約 1.75。
- 卡片標題與技能名稱：18px以上。
- 數值名稱與數值：15px以上。
- 技能類型、解鎖資訊與署名：14px以上。
- 側邊欄主要項目：16px。

最小輔助文字不得回到約 11～12px。

## 4. 實作原則

- 使用共用 typography tokens，不在各頁零散硬編碼。
- 名稱、正文、數值、條件與署名維持明確階層。
- 文字放大時允許卡片自然增高，不裁切、不覆蓋。
- 台版長名稱與解鎖條件必須可換行。
- 閱讀尺寸不等同瀏覽器縮放，不改變遊戲資料內容。
- 五套配色與亮／暗外觀共用相同文字尺度。

## 5. 驗收

- 所有 route 無水平溢位。
- 職業技能展開後的說明、數值與署名清楚可讀。
- 手機、平板、桌面皆可切換三種閱讀尺寸。
- 偏好重新整理後保留，跨 route 不重設。
- 外觀設定面板可由鍵盤操作，並以 `aria-pressed` 表示選取。
- `comfortable` 為未保存偏好時的預設。
