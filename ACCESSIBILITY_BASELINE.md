# 法那提歐的愛爾琳手札｜全站無障礙基準

版本：1.0  
適用範圍：全站 route、側邊欄、手機抽屜、快速搜尋、生活技能、料理篩選、職業技能、主題設定、功能導覽、浮動控制與未來新模組。

## 1. 核心原則

1. 任何資料狀態不得只靠顏色傳達。
2. 任何操作必須可以只用鍵盤完成。
3. 鍵盤焦點在所有明暗外觀與配色主題中都必須清楚可見。
4. 宣告為 modal 的介面必須限制焦點於面板內，關閉後回到原觸發位置。
5. 手機抽屜與隱藏篩選器在收起時不得保留可 Tab 到的隱形控制。
6. 啟用「減少動態效果」時，操作不得依賴平滑捲動、旋轉、淡入或位移動畫。
7. 固定浮動按鈕不得遮住頁尾重要文字或手機安全區。

## 2. 非色彩狀態規範

### 資料狀態

狀態徽章固定同時提供：

- 完整文字
- 邊框
- 狀態符號
- 配色

| 狀態 | 文字範例 | 符號 |
|---|---|---|
| 台版已確認 | 台版遊戲資料 | ✓ |
| 玩家實測 | 法那提歐實測 | ★ |
| 待補／待實測 | 台版待實測 | … |
| 異版參考 | 韓國版參考 | ↗ |
| 待確認 | 待確認 | ? |

符號只是輔助；文字永遠不可省略。

### 選取狀態

分類、技能與料理等級選取狀態至少同時具備：

- `aria-pressed="true"`
- 較強邊框或內側線條
- 勾選符號或其他形狀提示
- 原有文字

### 五大人物素質

力量、技巧、智力、意志、幸運保留遊戲語意色，但同時保留：

- 屬性文字
- 不同形狀記號
- 邊框或內側色條

不得以顏色單獨取代屬性名稱。

## 3. 鍵盤焦點

所有可互動元素使用一致的 `:focus-visible` 樣式：

- 至少 3px 外框
- 與周邊背景至少 3:1 的可辨識差異
- 不被 `outline: none` 移除
- 在側邊欄、亮色、暗色與柔和高對比中都能看見

需涵蓋：

- 連結
- 按鈕
- 搜尋輸入欄
- 下拉選單
- `<summary>`
- 自訂 `tabindex` 元素

## 4. Modal 與焦點管理

主題設定與功能導覽均使用 modal 行為：

- 開啟時背景工作區設為 `inert`
- 焦點移入面板
- `Tab` 與 `Shift+Tab` 在面板內循環
- `Escape` 可關閉
- 關閉後焦點回到原觸發按鈕

## 5. 響應式側邊欄

### 桌面與平板

- 側邊欄可正常 Tab 操作
- 目前 route 使用 `aria-current="page"`

### 手機

- 抽屜關閉時使用 `inert` 與 `aria-hidden="true"`
- 抽屜開啟時移除 `inert` 並改為 `aria-hidden="false"`
- 章節切換後抽屜自動關閉，焦點不留在不可見區域

### 料理情境篩選

- 離開料理頁後，隱藏的等級篩選器設為 `inert`
- 每個等級按鈕使用 `aria-pressed`

## 6. 展開與收合

職業技能 `<details>` 的 `<summary>` 需同步：

- `aria-expanded`
- `aria-controls`
- 對應內容區塊 ID
- 展開狀態的非色彩視覺提示

## 7. 對比基準

一般文字與主要背景至少 4.5:1；大型文字至少 3:1；焦點指示與元件邊界至少 3:1。

自動驗證至少量測：

- `--ink-900` 對 `--surface-solid`
- `--ink-700` 對 `--surface-solid`
- `--sidebar-text` 對 `--sidebar`
- `--focus-ring` 對 `--surface-solid`

所有五套配色的亮色與暗色都需通過。

## 8. 減少動態效果

`prefers-reduced-motion: reduce` 啟用時：

- 關閉平滑捲動
- 將必要以外動畫縮短至近乎零
- 回到頁首直接移動
- 生活技能詳情捲動不得強制平滑
- 停用不必要的 backdrop blur

## 9. 浮動控制與安全區

回到頁首按鈕需：

- 避開 `safe-area-inset-right`
- 避開 `safe-area-inset-bottom`
- 不覆蓋頁尾主要文字
- 保持至少 44×44px 的可點擊範圍

## 10. 人工鍵盤驗收清單

### 全站

- [ ] 從網址列進入後按 `Tab`，可看到「跳到主要內容」。
- [ ] 所有焦點框清楚，沒有消失在背景中。
- [ ] `Enter` 或空白鍵可觸發按鈕。
- [ ] 目前 route 有文字與形狀辨識，不只靠顏色。

### 快速搜尋

- [ ] 鍵盤可開啟快速搜尋。
- [ ] 開啟後焦點進入輸入欄。
- [ ] `Enter` 可送出。
- [ ] `Escape` 可關閉並回到放大鏡。

### 手機抽屜

- [ ] 關閉時 Tab 不會進入抽屜內。
- [ ] 開啟後可依序走訪章節連結。
- [ ] 選擇章節後抽屜關閉。

### 生活技能與料理

- [ ] 分類與項目可用鍵盤切換。
- [ ] `aria-pressed` 與畫面選取狀態一致。
- [ ] 料理等級在手機抽屜內仍可操作。

### 職業技能

- [ ] `<summary>` 可用 Enter／空白展開。
- [ ] `aria-expanded` 即時更新。
- [ ] 解鎖條件維持正常可讀。

### 主題與導覽 modal

- [ ] Tab 不會離開面板。
- [ ] Shift+Tab 可由第一個控制回到最後一個。
- [ ] Escape 關閉後焦點回到觸發按鈕。

### 減少動態效果

- [ ] 系統開啟減少動態效果後，不再出現平滑捲動。
- [ ] 操作仍可完整完成。

## 11. 自動驗證

```bash
node --check a11y-enhancements.js
npm run test:e2e
```

GitHub Actions 需執行：

- ARIA 狀態檢查
- 鍵盤操作流程
- modal 焦點限制
- 手機抽屜 inert
- 五套配色亮暗對比
- 非色彩狀態提示
- 減少動態效果
- 浮動按鈕安全區
