# 裝備、地圖、任務與活動共用架構契約

本文件定義《法那提歐的愛爾琳手札》新增裝備、地圖、任務與活動四類內容時共用的 route、資料欄位、導覽、搜尋與空狀態規則。

本契約只建立資訊架構，不公開示範資料，也不以韓版內容或推測數值填滿空頁。

## 1. Route namespace

| 模組 | 列表 route | 詳情 route |
| --- | --- | --- |
| 裝備 | `#/equipment` | `#/equipment/:id` |
| 地圖 | `#/maps` | `#/map/:id` |
| 任務 | `#/quests` | `#/quest/:id` |
| 活動 | `#/events` | `#/event/:id` |

規則：

- 列表與詳情必須分離。
- 每筆正式資料必須使用穩定英文 ID，形成可直接分享的 URL。
- 未知或尚未收錄的詳情 ID 顯示自然空狀態，不能回到首頁假裝成功。
- route 不得綁定顯示順序、目前活動日期或臨時分類名稱。

## 2. 共用欄位

四類資料至少共用下列欄位：

- `id`：穩定英文 ID。
- `name`：台版正式名稱。
- `category`：模組內分類。
- `summary`：列表摘要。
- `details`：詳情內容。
- `requirements`：解鎖、進入或參加條件。
- `status`／`statusLabel`：依既有資料狀態契約呈現。
- `contributor`：核准公開的整理者或投稿者名稱。
- `aliases`：只用於搜尋的舊稱、異版名稱或常見誤稱。
- `updatedAt`：資料更新日期。

未確認欄位必須留空或明確標示待確認，不得使用零、問號數值或韓版內容代填。

## 3. 模組專屬欄位

### 裝備

- 裝備部位／武器類型
- 稀有度
- 使用職業或限制
- 基礎能力
- 特殊效果
- 強化／製作／取得資訊

### 地圖

- 區域分類
- 進入條件或建議等級
- 地點描述與辨識提示
- 採集物／怪物／NPC
- 掛機注意事項

### 任務

- 任務類型
- 前置／解鎖條件
- 接取地點與 NPC
- 目標步驟
- 獎勵

### 活動

- 開始與結束時間
- 參加條件
- 活動流程
- 每日／每週限制
- 獎勵與兌換資訊
- 進行中、已結束或待確認狀態

## 4. 跨模組關聯

關聯只保存穩定 ID，不複製另一模組的完整內容。

- 裝備可連結來源地圖、任務、活動與適用職業。
- 地圖可連結生活技能、料理材料、NPC、任務與活動。
- 任務可連結地圖、NPC、裝備獎勵與前後置任務。
- 活動可連結期間任務、地圖、裝備與獎勵道具。

若被連結資料尚未公開，介面應顯示「關聯資料待收錄」，不得產生無效連結。

## 5. 導覽

- 側邊欄以一個可折疊的「世界與冒險」群組容納四個入口。
- 進入任一四類 route 時，群組自動展開並標示目前章節。
- 手機抽屜保持可捲動，不因新增四個入口而長期佔用畫面。
- 首頁另設四張章節入口卡，不與既有生活技能及職業入口混在同一區塊。

## 6. 搜尋

搜尋索引需顯示模組類型，並遵循：

- 裝備：正式名稱、類型、限制、效果與取得方式。
- 地圖：正式名稱、採集物、怪物、NPC 與別名。
- 任務：正式名稱、NPC、地圖、條件與獎勵。
- 活動：正式名稱、期間、條件與獎勵。

Issue #12 階段只索引四個章節入口，不建立不存在的遊戲資料搜尋結果。

## 7. 空狀態

空狀態必須清楚回答：

1. 架構已建立。
2. 目前沒有核准的台版資料。
3. 下一張施工 Issue 是哪一張。
4. 不會用韓版資料或假資料填滿頁面。

直接開啟不存在的詳情 ID 時，需顯示該 ID、返回列表入口與待收錄說明。

## 8. 公開資料安全

- 不提交私人 Google Drive 連結、原始截圖檔名、帳號資訊或內部工作日誌。
- 不把完整遊戲截圖當成裝飾背景。
- 圖片資產需另依公開資產契約核准。
- 活動日期與台版狀態屬時間敏感資料，未確認時不得推測。

## 9. Issue #12 完成定義

- 四個列表 route 與四種詳情 route 均可直接開啟。
- 側邊欄折疊群組、首頁入口與搜尋入口皆可使用。
- 空狀態不含假資料。
- `data/content-modules.json` 與本契約一致。
- 自動驗證與 Playwright 桌面、平板、手機回歸通過。
- changelog 已更新。
