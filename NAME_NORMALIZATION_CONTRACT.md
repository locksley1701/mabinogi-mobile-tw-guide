# 法那提歐的愛爾琳手札｜正式名稱與搜尋別名契約

版本：1.0  
適用範圍：`data/names.json`、全站快速搜尋、完整搜尋頁與未來裝備／地圖／任務／活動模組。

## 1. 核心原則

1. 頁面標題、搜尋結果標題與詳情內容一律使用台版正式名稱。
2. 舊稱、其他版本名稱、常見誤稱、玩家俗稱與簡稱只作搜尋入口，不取代正式名稱。
3. 名稱相似不代表同一項目；每筆別名必須指向穩定的 `targetType` 與 `targetId`。
4. 名稱對照不重新成為側邊欄主要頁面，也不在首頁堆疊流水帳。
5. 韓國版名稱只能作 `other-version` 搜尋別名或候選參考，不得覆蓋台版名稱。
6. 搜尋命中別名時，可提示使用者「由曾用名／其他版本名稱找到」，但正式標題保持不變。

## 2. `data/names.json` 結構

```json
{
  "id": "life-daily-gathering",
  "targetType": "life",
  "targetId": "daily-gathering",
  "scope": "entity",
  "canonical": "日常採集",
  "aliases": [
    {"name": "無工具採集", "kind": "former-name"}
  ],
  "source": "台版遊戲內容"
}
```

### 必填欄位

| 欄位 | 說明 |
|---|---|
| `id` | 名稱定義的唯一 ID |
| `targetType` | 搜尋分類，必須與全站搜尋類型一致 |
| `targetId` | 目標內容的穩定識別碼，不使用顯示順序或模糊文字搜尋 |
| `scope` | 這個正式名稱是目標本身，或只是目標內的相關搜尋詞 |
| `canonical` | 台版正式名稱或正式用語 |
| `aliases` | 一個以上的搜尋別名 |

### `scope` 允許值

| 值 | 語意 | 搜尋標題處理 |
|---|---|---|
| `entity` | 目標項目本身的正式名稱 | 可將舊資料中的顯示名稱正規化為 `canonical` |
| `related-term` | 目標內容內的材料、工具、地名或用語 | 只加入搜尋關鍵字，不改變項目標題 |

範例：

- `日常採集`、`鍊金術`、`擠著吃的點心` 使用 `entity`。
- `毅力草`、`伐木斧`、`昆蟲捕捉網` 使用 `related-term`，因為搜尋結果仍應顯示生活技能或攻略名稱。

### `targetType` 允許值

- `life`
- `cooking`
- `afk`
- `profession`
- `combatSkill`

新模組加入搜尋前，需先擴充契約與驗證器，不可自行創造未記錄類型。

### 別名類型 `kind`

| 值 | 公開提示 | 使用情境 |
|---|---|---|
| `former-name` | 曾用名 | 早期網站、舊版資料或已更正名稱 |
| `other-version` | 其他版本名稱 | 韓國版、其他地區版本或異版翻譯 |
| `common-typo` | 常見誤稱 | 常見錯字、用字混淆 |
| `colloquial` | 玩家俗稱 | 玩家社群常用的非正式稱呼 |
| `abbreviation` | 常用簡稱 | 不會與其他內容衝突的簡稱 |

## 3. 目標識別規則

### 生活技能

- `targetId` 使用 `life-skill-categories.json` 的分類 ID。
- 同分類的練等攻略共用該 ID，因此搜尋材料、工具或舊稱時可命中相關攻略。

範例：

- `daily-gathering`
- `herbalism`
- `mining`

### 料理

- `targetId` 使用 `cooking.json` 的料理 ID。
- 兩道名稱相近的料理必須使用不同 ID，不得因字面相似而合併。

### 職業

- `targetId` 使用 `professions.json` 的職業 ID。

### 職業技能

- 使用 `<professionId>:<skillName>`，例如 `swordsman:疾風斬`。
- 若未來技能取得獨立永久 ID，需另開契約版本後遷移。

## 4. 搜尋行為

- 搜尋會正規化 Unicode 全形／半形、英文字母大小寫、常見分隔符與多餘空白。
- 正式名稱、介紹、關鍵字與別名共同參與比對。
- 別名直接附加到正式搜尋項目，不建立會被去重丟棄的複製項目。
- `entity` 名稱可修正搜尋項目的正式標題；`related-term` 只補充關鍵字。
- 搜尋結果以正式名稱顯示。
- 命中別名時顯示：
  - `由曾用名「無工具採集」找到`
  - `由其他版本名稱「藏藏花」找到`
- 搜尋正式名稱時不顯示別名提示。

## 5. 不得合併的近似名稱

下列情況必須視為不同內容：

- `旅行者點心` 與 `擠著吃的點心`
- `戰士` 與 `大劍戰士`
- 名稱只差一兩個字，但遊戲內具有不同 ID、效果或分類的項目

新增別名前必須確認：

1. 是否真的是同一個遊戲項目。
2. 是否只是名稱相似。
3. 是否會讓過短俗稱同時命中多個不相關內容。

若無法確認，先不加入別名。

## 6. 資料維護流程

1. 以台版畫面確認正式名稱。
2. 確認目標類型與穩定 ID。
3. 判斷為實體名稱或相關搜尋詞。
4. 判斷別名種類。
5. 更新 `data/names.json`。
6. 新增或更新代表性搜尋案例。
7. 執行：

```bash
node scripts/validate-data-contract.mjs
node scripts/validate-name-normalization.mjs
npm run test:e2e
```

8. 更新 changelog。

## 7. 驗收門檻

- 所有別名均指向存在的目標 ID。
- 所有名稱定義均明確區分 `entity` 與 `related-term`。
- 同一別名不得指向不同內容。
- 別名不得與正式名稱相同。
- 至少保留 10 個代表性搜尋案例。
- 近似但不同的內容具有防誤合併案例。
- 手機與桌面搜尋結果不產生水平捲動。
- 公開頁面不顯示內部判讀證據、原始截圖檔名或私人 Drive 路徑。
